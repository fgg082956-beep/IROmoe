# ============================================================
# requirements.txt — зависимости проекта (v3)
# ============================================================

# --- Основные (были раньше) ---
requests==2.32.3
pandas==2.2.3
numpy==2.1.3

# --- Новостной сентимент (НОВОЕ) ---
feedparser==6.0.11       # парсинг RSS-лент Google News и MOEX
transformers==4.46.3     # HuggingFace: загрузка модели сентимент-анализа
torch==2.5.1             # движок для transformers (CPU-версия, ~700 МБ)
schedule==1.2.2          # планировщик запуска каждый час

# --- Опционально: Т-Инвестиции (реалтайм-котировки) ---
# tinkoff-investments==0.2.0b76
