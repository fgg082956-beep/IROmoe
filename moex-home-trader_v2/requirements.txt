# ============================================================
# requirements.txt — зависимости проекта (v4)
# ============================================================

# --- Основные ---
requests==2.32.3
pandas==2.2.3
numpy==2.1.3

# --- Новостной сентимент ---
feedparser==6.0.11       # парсинг RSS-лент Google News и MOEX
transformers==4.46.3     # HuggingFace: модель сентимент-анализа
torch==2.5.1             # движок для transformers (CPU-версия, ~700 МБ)
schedule==1.2.2          # планировщик: запуск каждый час

# --- Параллельность (stdlib, доп. установка не нужна) ---
# concurrent.futures — входит в стандартную библиотеку Python 3.2+

# --- Опционально: Т-Инвестиции (реалтайм-котировки) ---
# t-tech-invest==0.2.0b76   # раскомментировать, если USE_TBANK=True
# Установка: pip install t-tech-invest
